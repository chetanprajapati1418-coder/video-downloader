# Video Downloader - Render Ready

Build command:
pip install -r requirements.txt

Start command:
gunicorn app:app

Important: FFmpeg is required when yt-dlp needs to merge separate video and audio streams. Use a deployment environment/container where FFmpeg is installed and available on PATH.

Use only content you are authorized to download and follow applicable platform terms and copyright law.
